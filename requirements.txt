# Collecteur
requests==2.34.2

# Console web (service `web` de compose.yaml, voir app/web/). Le collecteur n'en a pas
# besoin, mais les deux services partagent la même image : une seule liste, un seul build.
fastapi==0.141.1
jinja2==3.1.6
uvicorn==0.52.4
